import { StyleSheet } from 'react-native';

// Layaout

const styles = StyleSheet.create({
  screen: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center ',
  },
  text: {
    _colr: 'blue',
  },
});

// WelcomeScreen
// LoginScreen
// HomeScreen

const saldoText = StyleSheet.create({
  fontSize: 30,
  padding: 20,
});

export { styles, saldoText };
